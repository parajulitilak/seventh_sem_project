{
    "builds": [{
        "src": "summarizer_project/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.11.8" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "summarizer_project/wsgi.py"
        }
    ]
}
